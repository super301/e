/*
 * 工具类：用于读取ImageIcon、计算HashMap<Food, Integer>的总价格
 */
package e.common;
import javax.swing.ImageIcon;
public class ImageTool {

	public ImageIcon readImage(String path) {
		return new ImageIcon();
	}
}
